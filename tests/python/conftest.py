"""tests/python pytest entry — shared fixtures come from the root conftest.

The fixtures themselves live in tests/python/harness_fixtures.py (a
plain module, not a conftest). The plugin is declared ONCE in the
top-level conftest.py (pytest >= 9 requires pytest_plugins there).
"""
